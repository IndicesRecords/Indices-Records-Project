import { defineConfig } from 'sanity';

export default defineConfig({
  projectId: 'nnxl5s4y', // Replace with your actual Project ID
  dataset: 'production',       // Replace if using a different dataset
  title: 'Indices Records',
  plugins: [],
});
